# ats-boards-cli

Search public **Greenhouse**, **Lever**, and **Ashby** job boards. No API key. Zero runtime dependencies.

Board tokens are the public careers-page slug (`jobs.ashbyhq.com/openai` → `openai`, `boards.greenhouse.io/stripe` → `stripe`).
