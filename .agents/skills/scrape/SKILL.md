---
name: scrape
description: Find new matching job postings through installed portal-search skills, deduplicate them, and present results by fit.
---

Read `.claude/skills/job-scraper/SKILL.md` completely and follow it as the canonical workflow. Interpret `/scrape` as `$scrape`, use `AGENTS.md`, and find portal CLIs under `.agents/skills/*-search/`.
