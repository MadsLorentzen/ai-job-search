---
name: add-template
description: Register and verify a custom CV or cover-letter template and connect it to the application workflow.
---

Read `.claude/commands/add-template.md` completely and follow it as the canonical workflow. Interpret `/add-template` as `$add-template` and use `AGENTS.md`.
