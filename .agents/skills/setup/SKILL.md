---
name: setup
description: Build, import, reset, or update the candidate profile for this job-search workspace. Use for onboarding, profile calibration, and setup section updates.
---

Read `.claude/commands/setup.md` completely and follow it as the canonical workflow. Interpret `/setup` as `$setup` and use the Codex capability mapping in `AGENTS.md`.
