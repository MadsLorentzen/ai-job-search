---
name: reset
description: Reset selected candidate-profile or job-search workspace data when the user explicitly requests it.
---

Read `.claude/commands/reset.md` completely and follow it as the canonical workflow. Interpret `/reset` as `$reset`. Resolve the exact scope and obtain confirmation before destructive changes.
