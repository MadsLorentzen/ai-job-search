---
name: notion-sync
description: Publish a one-way view of ranked jobs and applications to Notion while keeping repository files authoritative.
---

Read `.claude/commands/notion-sync.md` completely and follow it as the canonical workflow. Interpret `/notion-sync` as `$notion-sync`. Require an authenticated Notion connector and approval before external writes.
