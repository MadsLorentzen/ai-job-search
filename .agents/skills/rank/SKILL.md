---
name: rank
description: Score and rank scraped job postings against the candidate profile, preserving strengths, gaps, deal-breakers, deadlines, and state.
---

Read `.claude/commands/rank.md` completely and follow it as the canonical workflow. Interpret `/rank` as `$rank` and use `AGENTS.md`.
